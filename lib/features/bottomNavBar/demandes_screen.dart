import 'package:flutter/material.dart';

class DemandesScreen extends StatelessWidget {
  const DemandesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Demandes Screen'));
  }
}
